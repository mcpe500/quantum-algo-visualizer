# SPEC-043: Shared Gate Library, QFT Video Export Parity, QAOA Hybrid Hardening

## Prompt
Refactor quantum animation system across all 4 algorithms (DJ, QFT, QAOA, VQE) with four objectives:
1. **Issue 1**: Make ALL currently-used gate components shared — not just HadamardGate. Extract GateTile, GateNode, GateDisc, ControlDot, TargetMarker, QubitOrb variants into `shared/components/3d/gates/`
2. **Issue 2**: Add video export (WebM + MP4) to QFT animation — QFT previously lacked export despite having `video-overlay.ts`
3. **Issue 3**: QAOA hybrid visualization hardening — remove hardcoded "1024" shot counts, make pseudocode dynamic from `data.shots`
4. **Architectural**: Create a shared parent scene orchestrator where DJ/QFT are quantum-only, QAOA/VQE are hybrid, with shared gate primitives and unified video export pipeline

## Tujuan
- Semua gate yang sudah dipakai (GateTile, GateNode, GateDisc, ControlDot, TargetMarker, QubitOrb) menjadi shared component
- QFT punya export video WebM + MP4 setara DJ/QAOA
- QAOA hybrid panel 100% dinamis (tidak ada hardcoded shot count)
- Satu entry scene orchestrator untuk dispatch DJ/QFT/QAOA

## Codebase Context
- Frontend: React + Vite + TypeScript + React Three Fiber + Tailwind
- Shared components di `frontend/src/shared/components/3d/`
- DJ benchmark untuk kualitas animasi (sudah OK)
- VQE belum punya Animasi 3D tab, tapi dipertimbangkan di desain

## Perubahan yang Dilakukan

### Phase 1 — Shared Gate Library
Membuat `frontend/src/shared/components/3d/gates/` berisi 6 komponen gate:

| Komponen | Asal | Geometry | Algorithms Using |
|----------|------|----------|------------------|
| `LabeledBoxGate` | DJ `GateTile` | boxGeometry | DJ (X, M) |
| `LabeledSphereGate` | QFT `GateNode` | sphereGeometry | QFT (C, S) |
| `LabeledDiscGate` | QAOA `GateDisc` | cylinderGeometry | QAOA (C, ZZ, Rx, M) |
| `ControlDot` | DJ `ControlDot` | sphereGeometry | DJ (● marker) |
| `TargetMarker` | DJ `TargetMarker` | torus+lines | DJ (⊕ marker) |
| `QubitOrb` | QFT+QAOA `QubitOrb` | sphereGeometry | QFT (phase), QAOA (probability) |

Setiap komponen:
- Props konsisten: `x`, `y`, `isActive`, `label`, `color`
- Props opsional: `size`, `radius`, `height`, `zIndex`, `segments`, `opacity`
- Style aktif/inaktif identik dengan aslinya

Refactor konsumen:
- `dj/animation/scene-column.tsx` — hapus definisi lokal GateTile/ControlDot/TargetMarker, import dari shared
- `qft/animation/scene-primitives.tsx` — hapus definisi lokal GateNode/QubitOrb, import dari shared
- `qaoa/animation/scene-primitives.tsx` — hapus definisi lokal GateDisc/QubitOrb, import dari shared

### Phase 2 — Shared Scene Orchestrator
Membuat `frontend/src/components/quantum/QuantumSceneOrchestrator.tsx`:
- Props: `algorithm`, `data`, `currentStep`, `cameraMode`
- Dispatch internal ke `DJStoryScene`, `QFTStoryScene`, `QAOAStoryScene`
- QAOA melakukan lookup `activeStep = data.timeline[currentStep]` sebelum dispatch
- Barrel export di `components/quantum/index.ts`

### Phase 3 — QAOA Hybrid Hardening
Menghapus semua hardcoded "1024" di UI QAOA:
- `QAOAHybridAnimation.tsx` DetailCard hint: `"1024 shots."` → `` `${data.shots} shots.` ``
- `video-overlay.ts` intro detail: `"measurement 1024 shots"` → `` `measurement ${data.shots} shots` ``
- `HybridFlowPanel.tsx` pseudocode: `"AerSimulator(shots=1024)"` → `` `AerSimulator(shots=${data.shots})` ``
- Array `PSEUDOCODE_LINES` dipindahkan ke dalam body komponen agar bisa akses `data.shots`
- Hybrid flow arrows, box states, pseudocode highlight sudah 100% driven oleh `activeStep.phase`
- Parameter gamma/beta sudah 100% dari `checkpoint` backend data

### Phase 4 — QFT Video Export Parity
Menambahkan pipeline export penuh ke `QFTQuantumAnimation.tsx`:
- State: `isExporting`, `isConverting`, `exportError`
- Refs: `exportRendererCanvasRef`, `exportAnimationFrameRef`
- Derived: `supportedVideoMimeType`, `ffmpegReady`
- Handlers: `runExportPipeline('webm'|'mp4')`, `handleExportVideo`, `handleExportMp4`
- Guards: semua handler playback memiliki `if (isExporting) return;`
- UI: tombol WebM 1080p (teal) + MP4 1080p (emerald) di playback controls
- Off-screen canvas: 1920x1080, `preserveDrawingBuffer`, render `<QFTStoryScene>`
- Filename prefix: `qft_${data.case_id}_video-quantum.{webm,mp4}`
- Membuat file `qft/animation/helpers.ts` yang sebelumnya MISSING (re-export dari `shared/utils`)
- Menghapus dead code `isFFmpegSupported` dari `qft/animation/video-overlay.ts`

## Validasi & Audit

### Critic Findings & Fixes
1. **CRITICAL BUILD FAILURE**: `shared/components/index.ts` tidak mengekspor gate baru. Fix: tambahkan `export * from './3d';`
2. **QFT PhaseStepper disabled**: harus `isExporting || isConverting` untuk parity DJ. Fix: applied.
3. **QFT ReadingGuideCard totalSteps**: QFT component tidak support prop ini. Fix: tidak ditambahkan.
4. **Dead code QFT**: `isFFmpegSupported` di `video-overlay.ts` tidak dipakai. Fix: dihapus.
5. **Unused imports**: `Text` di DJ scene-column, `React` di 6 gate files, `PHASE_COLOR` di HybridFlowPanel. Fix: dihapus semua.

### Build Verification
- `tsc --noEmit`: ✅ PASS
- `npm run build`: ✅ PASS (28.47s, no errors)

### Perbandingan dengan Rencana Awal
| Item Rencana | Status | Catatan |
|--------------|--------|---------|
| Shared Gate Library (semua gate yang sudah dipakai) | ✅ COMPLETE | 6 komponen, semua konsumen direfactor |
| Shared Scene Orchestrator (1 entry file) | ✅ COMPLETE | QuantumSceneOrchestrator.tsx |
| QAOA Hybrid Hardening (dinamis, no hardcode) | ✅ COMPLETE | Zero hardcoded "1024" di UI |
| QFT Video Export Parity (WebM + MP4) | ✅ COMPLETE | Full pipeline identik DJ |
| tsc --noEmit | ✅ PASS | |
| npm run build | ✅ PASS | |

## Files Created / Modified

### New Files
- `frontend/src/shared/components/3d/gates/LabeledBoxGate.tsx`
- `frontend/src/shared/components/3d/gates/LabeledSphereGate.tsx`
- `frontend/src/shared/components/3d/gates/LabeledDiscGate.tsx`
- `frontend/src/shared/components/3d/gates/ControlDot.tsx`
- `frontend/src/shared/components/3d/gates/TargetMarker.tsx`
- `frontend/src/shared/components/3d/gates/QubitOrb.tsx`
- `frontend/src/shared/components/3d/gates/index.ts`
- `frontend/src/components/quantum/QuantumSceneOrchestrator.tsx`
- `frontend/src/components/quantum/index.ts`
- `frontend/src/components/qft/animation/helpers.ts`

### Modified Files
- `frontend/src/shared/components/3d/index.ts`
- `frontend/src/shared/components/index.ts`
- `frontend/src/components/dj/animation/scene-column.tsx`
- `frontend/src/components/qft/animation/scene-primitives.tsx`
- `frontend/src/components/qft/QFTQuantumAnimation.tsx`
- `frontend/src/components/qft/animation/video-overlay.ts`
- `frontend/src/components/qaoa/animation/scene-primitives.tsx`
- `frontend/src/components/qaoa/QAOAHybridAnimation.tsx`
- `frontend/src/components/qaoa/animation/video-overlay.ts`
- `frontend/src/shared/components/panels/HybridFlowPanel.tsx`

## Manual Testing Plan
1. Buka DJ Animasi 3D — verify gate X, M, ●, ⊕, H render correctly
2. Buka QFT Animasi 3D — verify gate C, S, H, phase orbs render correctly
3. Buka QAOA Animasi 3D — verify gate C, ZZ, Rx, M, probability orbs render correctly
4. QFT — klik WebM 1080p, verify download starts
5. QFT — klik MP4 1080p (Chrome/Edge), verify conversion + download
6. QAOA — verify hybrid panel shows correct shots count (try different shots in backend)
7. QAOA — verify pseudocode AerSimulator line shows correct shots count
