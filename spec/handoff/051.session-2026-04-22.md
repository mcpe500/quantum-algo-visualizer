# Handoff Session 051 - 2026-04-22

## Task
QAOA adjacency-matrix-only JSON schema - ubah seluruh stack agar JSON hanya berisi `adjacency_matrix`, backend strict validation + derive nodes/edges, frontend matrix-native components.

## Status
COMPLETE - BUILD PASS

## What Was Done

### 1. Backend - Strict Matrix Validation
- `_validate_adjacency_matrix()`: Validasi square, symmetric, diagonal=0, values 0/1
- `_normalize_problem()`: Matrix-centric rewrite - derives nodes/edges/edges_weighted dari adjacency_matrix
- `enrich_case_graph()`: Inject derived nodes/edges ke API response untuk backward compatibility
- Removed legacy node/edge parsing logic (`_edge_from_raw` still exists but unused by `_normalize_problem`)

### 2. Backend - Routes Updated
- `GET /api/qaoa/cases`: Setiap case di-enrich dengan derived nodes/edges
- `GET /api/qaoa/dataset/<case_id>`: Case di-enrich sebelum response

### 3. Frontend Utils - New File
- `frontend/src/utils/qaoaGraph.ts`: `deriveNodesFromMatrix`, `deriveEdgesFromMatrix`, `deriveGraphFromMatrix`

### 4. Frontend Types Updated
- `QAOAGraph`: `adjacency_matrix` required, `nodes`/`edges` optional (backend may provide via enrichment)

### 5. Frontend Components - Matrix-Native Rewrite
- `AdjacencyMatrixGrid.tsx`: Hanya accept `adjacencyMatrix: number[][]` prop, derive nodes internally
- `MiniGraph.tsx`: Hanya accept `adjacencyMatrix: number[][]` prop, derive nodes/edges via `deriveGraphFromMatrix`
- `QAOADatasetVisualizer.tsx`: Derive nodes/edges dari matrix via `useMemo`, pass `adjacencyMatrix` ke children

## Files Changed
- `backend/services/qaoa_service.py`
- `backend/api/routes/qaoa.py`
- `frontend/src/utils/qaoaGraph.ts` (NEW)
- `frontend/src/types/qaoa.ts`
- `frontend/src/components/qaoa/AdjacencyMatrixGrid.tsx`
- `frontend/src/components/qaoa/MiniGraph.tsx`
- `frontend/src/components/qaoa/QAOADatasetVisualizer.tsx`
- `spec/050.qaoa-adjacency-matrix-centric-schema.md`
- `spec/handoff/051.session-2026-04-22.md`

## Verification
- `npm run build` -> PASS (31.67s)
- TypeScript compilation -> PASS (0 errors)

## Data Flow
```
JSON (adjacency_matrix only)
  → Backend _normalize_problem (validate + derive nodes/edges)
  → Backend enrich_case_graph (inject derived data for API)
  → API Response (adjacency_matrix + nodes + edges)
  → Frontend QAOADatasetVisualizer (derive from matrix locally)
  → AdjacencyMatrixGrid / MiniGraph (matrix-native)
```

## Notes
- `_edge_from_raw` function masih ada tapi tidak dipakai `_normalize_problem` lagi
- Payload functions (benchmark, animation, classical) otomatis benar karena pakai `_normalize_problem`
- Backend strict validation: ValueError jika matrix tidak valid
- Frontend components tidak lagi bergantung pada nodes/edges dari JSON
