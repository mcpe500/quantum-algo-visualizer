# Handoff 018: DJ Animation Compact Layout

Date: 2026-04-16
Session: Compact GateTimeline + Canvas optimization

## Goal

DJ animation muat 1 layar tanpa scroll. Gate timeline tidak buang-buang vertikal space.

## Changes Made

### 1. GateTimeline → CompactGateTimeline (`DJQuantumAnimation.tsx`)
- Removed: `StageCard` component (~24 lines)
- Removed: `GateTimeline` component (~57 lines) with 2-col grid + per-phase group cards
- Added: `CompactGateTimeline` (~60 lines)
  - Row 1: Phase stepper — 5 badges inline + `ChevronRight` separators, clickable (jump to phase)
  - Row 2: Active stage detail — operation + wire markers inline
  - Height: ~60px (was ~400-600px)
- Added: `handleJumpPhase` callback in main component
- Added: `Fragment`, `ChevronRight` imports

### 2. Canvas Height (`DJQuantumAnimation.tsx`)
- Changed: 560px → 480px
- Rationale: save 80px vertikal, total page now fits ~1080px viewport

### 3. Collapsible QuantumTraceTable (`DJCombinedPage.tsx`)
- Wrapped `QuantumTraceTable` in `<details>` element
- Default collapsed, label shows gate count
- Eliminates page-level duplication with animation's gate info

### 4. Build Verification
- `vite build` passes
- Pre-existing `npm run build` error (QAOA `colNode`) unchanged — not from our changes

## Layout Estimate (after changes)

| Section | Height |
|---------|--------|
| Header (title) | ~40px |
| CompactGateTimeline | ~60px |
| Legend + token toggle | ~30px |
| Catatan note | ~30px |
| Canvas Three.js | 480px |
| Player controls | ~70px |
| **Total animation** | **~710px** |
| Page overhead (nav, selector, tabs) | ~180px |
| **Total page** | **~890px** |

Fits in 1080px viewport with margin.

## Remaining Tasks

1. **Visual verification** — manual test semua 4 DJ cases, semua phases, no overlap
2. **QFT dataset replacement** — QFT-01 → 28-point, QFT-02 → 64-point (not started)
3. **Pre-existing QAOA TypeScript error** — `QAOADatasetVisualizer.tsx(126,48): colNode unused`

## Files Changed

| File | Lines changed |
|------|---------------|
| `frontend/src/components/dj/DJQuantumAnimation.tsx` | Replaced GateTimeline+StageCard with CompactGateTimeline, canvas 480px |
| `frontend/src/pages/DJCombinedPage.tsx` | Wrapped QuantumTraceTable in collapsible details |
| `spec/018.dj-quantum-animation.md` | Updated Track B section, files changed, component list |
| `spec/handoff/018.session-2026-04-16.md` | This file |
