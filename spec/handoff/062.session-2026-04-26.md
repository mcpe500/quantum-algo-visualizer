# Handoff 062: DJ Step 1 Function and Bloch Clarity

## Summary

Fixed the new visual issue from global video/image notes. Deutsch-Jozsa Step 1 now shows the function directly in the 3D scene and exported video narration. Constant cases explicitly state `f(x) = 0 for all x` or `f(x) = 1 for all x`. Bloch sphere arrows are clearer and have more visible internal space.

## Changed Files

1. `frontend/src/components/dj/animation/narration.ts`
   - Added `getOracleFunctionLabel(data)`.
   - Step 1 export accent now shows `Function: ...`.

2. `frontend/src/components/dj/animation/scene-primitives.tsx`
   - Added `FunctionBoard`.
   - Board is rendered above DJ circuit lanes and highlighted on Step 1.

3. `frontend/src/shared/components/3d/gates/BlochQubitNode.tsx`
   - Enlarged Bloch sphere slightly.
   - Made sphere more transparent.
   - Enlarged arrow shaft/head.
   - Darkened arrow material for stronger direction cue.
   - Moved `performance.now()` out of render path to satisfy lint purity.

## Verification

Passed:

```powershell
npx.cmd eslint src\components\dj\animation\narration.ts src\components\dj\animation\scene-primitives.tsx src\shared\components\3d\gates\BlochQubitNode.tsx
npm.cmd run build
```

Note:

- `npm.cmd run typecheck` was not available because `frontend/package.json` has no `typecheck` script.
- Local dev server probe on port `5174` did not connect in this environment, so browser screenshot verification was not completed in this turn.

