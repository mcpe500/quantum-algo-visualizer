# Handoff Session 044 - 2026-04-21

## Task
Perbaikan correctness QFT animation agar sesuai proposal, dataset, dan referensi Qiskit.

## Status
COMPLETE - backend validation PASS, frontend build PASS

## What Was Done

### Backend
- Mengganti timeline QFT agar setiap langkah dihasilkan dari evolusi `Statevector` Qiskit yang riil.
- Menambahkan `qubit_summaries` per langkah berbasis reduced density matrix, sehingga frontend menerima parameter Bloch yang benar.
- Meningkatkan presisi serialisasi statevector agar validasi tidak gagal hanya karena pembulatan.
- Menambahkan validator baru `backend/validate_qft_timeline.py`.

### Frontend
- Mengubah QFT scene supaya memakai `qubit_summaries` langsung dari backend.
- Menambahkan dukungan `bodyColor` dan `coherence` pada `BlochQubitNode`.
- Memperbaiki urutan substep dan narasi agar controlled-phase muncul sebelum hadamard.
- Menyesuaikan tipe data QFT agar kontrak payload baru tervalidasi oleh TypeScript.

## Files Changed
- `backend/services/qft_service.py`
- `backend/validate_qft_timeline.py`
- `frontend/src/types/qft.ts`
- `frontend/src/components/qft/animation/scene-primitives.tsx`
- `frontend/src/components/qft/animation/phase-cascade.ts`
- `frontend/src/components/qft/animation/narration.ts`
- `frontend/src/shared/components/3d/gates/BlochQubitNode.tsx`

## Verification
- `.\\venv\\Scripts\\python.exe backend\\validate_qft_timeline.py --cases QFT-01 QFT-02`
- `node_modules\\.bin\\tsc.cmd -b --pretty false`
- `npm.cmd run build`

Semua perintah di atas lulus.

## Notes
- Mismatch utama sebelumnya berasal dari implementasi `phase_cascade` manual di backend, bukan dari renderer frontend semata.
- Dataset aktif yang tervalidasi eksplisit adalah `QFT-01` dan `QFT-02`.
- Frontend kini bergantung pada `qubit_summaries`; fallback pseudo-Bloch lama sudah tidak dipakai lagi.

## Next Steps
1. Jalankan validator yang sama setiap kali dataset QFT baru ditambahkan.
2. Lakukan pengecekan visual manual di browser untuk memastikan orb dan narasi sudah cocok dengan ekspektasi pengguna.

## Continuation Update - Boxed Gate-Centric Circuit Engine (2026-04-21)

### New Direction
Permintaan visual diubah: area circuit utama wajib gate-centric boxed design, tanpa bentuk utama sphere/disc/dot untuk gate.

### What Was Updated
- Semua representasi gate yang sebelumnya sphere/disc/dot/marker diubah menjadi box solid dengan label di dalam box.
- Controlled gate sekarang punya box pada semua qubit terlibat + koneksi vertikal.
- Multi-qubit gate ditampilkan sebagai grouped boxes yang terhubung vertikal.
- QFT timeline kini menampilkan kolom `ENC`, `H`, `CP`, `SWAP`, `M` sebagai box yang center-aligned pada wire.
- DJ oracle marker `●` dan `⊕` kini dirender sebagai box berlabel (`C` dan `X`), bukan titik/target cincin.
- QAOA gate `C`, `ZZ`, `RX`, `M` dirender sebagai box konsisten.

### Files Changed (Continuation)
- `frontend/src/shared/components/3d/gates/LabeledSphereGate.tsx`
- `frontend/src/shared/components/3d/gates/LabeledDiscGate.tsx`
- `frontend/src/shared/components/3d/gates/ControlDot.tsx`
- `frontend/src/shared/components/3d/gates/TargetMarker.tsx`
- `frontend/src/components/dj/animation/scene-column.tsx`
- `frontend/src/components/qft/animation/scene-primitives.tsx`
- `frontend/src/components/qaoa/animation/scene-primitives.tsx`

### Verification (Continuation)
- `npm.cmd run build` -> PASS
