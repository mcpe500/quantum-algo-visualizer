# Handoff Session 048 - 2026-04-21

## Task
Perbaiki semua issue alignment screenshot dataset visualization untuk DJ, QFT, dan QAOA, termasuk stabilitas pipeline `html2canvas`, relayout tombol kamera, centering angka/label, dan dokumentasi sesi lanjutannya.

## Status
COMPLETE - BUILD PASS

## What Was Done

### 1. Screenshot Pipeline Distabilkan
- `frontend/src/utils/download.ts` sekarang menunggu `document.fonts.ready`.
- Ditambah dua frame `requestAnimationFrame` sebelum capture agar layout dan paint settle.
- `html2canvas` sekarang memakai `logging: false`.
- `onclone` dipakai untuk mematikan animation/transition pada subtree target saat proses render clone.

### 2. Tombol Kamera Tidak Lagi Mengubah Layout
- Pada `OracleTopography.tsx`, `QFTSignalTopology.tsx`, dan `QAOADatasetVisualizer.tsx`, tombol kamera tidak lagi dihapus dari DOM saat capture.
- Tombol sekarang tetap dirender, tetapi diberi `data-html2canvas-ignore="true"` supaya tidak ikut muncul pada PNG.
- Selama cooldown, tombol hanya di-`disabled`, bukan di-unmount.
- Ini menutup sumber mismatch terbesar antara layout live dan layout hasil screenshot.

### 3. Alignment Angka Diperkuat
- DJ truth table: angka input/output dipaksa center dengan `inline-flex`, `leading-none`, dan `tabular-nums`.
- QFT badge/footer: label dan badge numerik dipertegas line-height dan centering-nya.
- QAOA header badge: bukan lagi `span` inline biasa. Sekarang memakai `HeaderBadge` dengan tinggi tetap dan `place-items-center` agar teks tidak jatuh ke bawah pada PNG.
- QAOA adjacency matrix: row label, column label, dan cell value diberi centering numerik berbasis `grid place-items-center` + offset baseline kecil.
- QAOA mini graph: label node SVG tidak lagi memakai offset manual `y + 4`; sekarang memakai `dy="0.35em"` yang lebih aman untuk html2canvas/SVG screenshot.
- QAOA footer label dan deskripsi juga diberi slot vertikal tetap.

### 4. Dokumentasi Baru Ditulis
- Spec baru: `spec/047.dataset-screenshot-alignment-hardening.md`
- Handoff baru: `spec/handoff/048.session-2026-04-21.md`

## Files Changed
- `frontend/src/utils/download.ts`
- `frontend/src/components/dj/OracleTopography.tsx`
- `frontend/src/components/qft/QFTSignalTopology.tsx`
- `frontend/src/components/qaoa/QAOADatasetVisualizer.tsx`
- `frontend/src/components/qaoa/AdjacencyMatrixGrid.tsx`
- `frontend/src/components/qaoa/MiniGraph.tsx`
- `spec/047.dataset-screenshot-alignment-hardening.md`
- `spec/handoff/048.session-2026-04-21.md`

## Verification
- `npm run build` -> PASS
- `npx eslint src/utils/download.ts src/components/dj/OracleTopography.tsx src/components/qft/QFTSignalTopology.tsx src/components/qaoa/QAOADatasetVisualizer.tsx src/components/qaoa/AdjacencyMatrixGrid.tsx src/components/qaoa/MiniGraph.tsx` -> PASS

## Notes
- Perbaikan ini sengaja minimal dan fokus pada area yang langsung memengaruhi hasil PNG untuk kebutuhan BUKU TA.
- Tidak ada redesign besar pada halaman dataset.
- Belum ada verifikasi visual otomatis karena capture masih berbasis browser interaction. Manual recapture tetap diperlukan untuk aset final bab 4.

## Next Steps
1. Regenerasi PNG di halaman `/dj/dataset`, `/qft/dataset`, dan `/qaoa/dataset`.
2. Ganti file gambar yang dipakai di `bab4/images-context.md` dengan hasil capture baru.
3. Jika masih ada kasus tepi pada browser tertentu, fokuskan pengecekan pada font loading dan baseline SVG text.
