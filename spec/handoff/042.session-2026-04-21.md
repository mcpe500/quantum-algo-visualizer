# Handoff 042 — Scene-Primitives Unified + QAOA HybridFix + DJ Tab Fix

**Date:** 2026-04-21
**Status:** ALL COMPLETE
**Related Spec:** 042.scene-unified-hybridfix-djtab.md

## Summary

Fixed 3 issues:
1. Extracted shared `CameraRig` and `PhaseBand` components from duplicated DJ/QFT/QAOA code
2. Redesigned QAOA hybrid visualization with `HybridFlowPanel` (flow diagram + pseudocode + arrows)
3. Added "Animasi 3D" tab to DJ page for consistency with QFT/QAOA

---

## Issue 1: Shared Scene-Primitives (CameraRig + PhaseBand)

### Problem

`CameraRig` and `PhaseBand` were duplicated in DJ and QFT scene-primitives files (100% identical code). QAOA also had its own `CameraRig`.

### Solution

Created shared components with configurable offsets:

| Component | File | Props |
|-----------|------|-------|
| `CameraRig` | `shared/components/3d/CameraRig.tsx` | `mode`, `distance`, `fixedOffset`, `orbitOffset`, `lookAtY` |
| `PhaseBand` | `shared/components/3d/PhaseBand.tsx` | `startX`, `endX`, `color`, `label`, `topY`, `height` |

### Algorithm-Specific Camera Offsets

| Algorithm | fixedOffset | orbitOffset | lookAtY |
|-----------|-------------|-------------|---------|
| DJ | `{0, -0.1}` | `{1.8, 1.2, 1.5}` | `-0.1` |
| QFT | `{0, -0.5}` | `{2, 1.5, 2}` | `-0.5` |
| QAOA | `{0, 0.4}` | `{1.8, 2, 2}` | `0` |

### Files Changed

| File | Action |
|------|--------|
| `shared/components/3d/CameraRig.tsx` | CREATED (33 lines) |
| `shared/components/3d/PhaseBand.tsx` | CREATED (38 lines) |
| `shared/components/3d/index.ts` | MODIFIED (added exports) |
| `shared/components/index.ts` | MODIFIED (added exports) |
| `dj/animation/scene-primitives.tsx` | MODIFIED (removed local CameraRig/PhaseBand, import shared) |
| `qft/animation/scene-primitives.tsx` | MODIFIED (removed local CameraRig/PhaseBand, import shared with QFT offsets) |
| `qaoa/animation/scene-primitives.tsx` | MODIFIED (removed local CameraRig, import shared with QAOA offsets) |

---

## Issue 2: QAOA HybridFlowPanel Redesign

### Problem

Previous `HybridLoopPanel` showed OUTPUT (parameter values, cut scores) but NOT the PROCESS. No visualization of the iterative hybrid loop.

### Solution

Created `HybridFlowPanel` with 3 sections:

1. **Flow Diagram**: 3 boxes (Classical/Quantum/Measure) with arrows
2. **Pseudocode**: 6 lines with active line highlighting based on current phase
3. **Parameters**: γ and β values for active checkpoint

### Phase-to-Line Mapping

| activePhase | Active Line | Description |
|-------------|-------------|-------------|
| `optimizer` | 4 | COBYLA receives -⟨H_C⟩, updates γ, β |
| `superposition` | 2 | Build QAOA circuit with γ, β |
| `cost` | 2 | Build QAOA circuit with γ, β |
| `mixer` | 2 | Build QAOA circuit with γ, β |
| `measurement` | 6 | Final: AerSimulator(shots=1024) → bitstring |
| `update` | 4 | COBYLA receives -⟨H_C⟩, updates γ, β |

### Files Changed

| File | Action |
|------|--------|
| `shared/components/panels/HybridFlowPanel.tsx` | CREATED (95 lines) |
| `qaoa/animation/panels.tsx` | MODIFIED (replaced HybridLoopPanel with HybridFlowPanel) |

---

## Issue 3: DJ Add "Animasi 3D" Tab

### Problem

DJ had only 2 tabs (Klasik/Kuantum) while QFT and QAOA had 3. `DJQuantumAnimation` with video export was embedded inside `quantumTab`.

### Solution

Added 3rd tab to DJ, moved animation to dedicated tab.

### Before/After

**Before:**
| Tab | Content |
|-----|---------|
| Klasik | ClassicalVisualization |
| Kuantum | DJQuantumAnimation + QuantumVisualization + TraceTable + Comparison |

**After:**
| Tab | Content |
|-----|---------|
| Klasik | ClassicalVisualization |
| Kuantum | QuantumVisualization + TraceTable + Comparison |
| Animasi 3D | DJQuantumAnimation (video export WebM/MP4) |

### Files Changed

| File | Action |
|------|--------|
| `hooks/useDJBenchmark.ts` | MODIFIED (activeTab type includes 'animation') |
| `pages/DJCombinedPage.tsx` | MODIFIED (added animationTab, moved DJQuantumAnimation) |

---

## Verification

- TypeScript: `tsc -b --noEmit` → 0 errors ✅
- No new ESLint errors ✅

## Next Steps (Optional)

1. Consider extracting `GenericGate` component to replace `GateTile`/`GateNode`/`GateDisc`
2. Consider creating unified `QubitOrb` component (discriminated union for phase vs pOne)
3. Consider building VQE animation system (currently non-existent)

## Notes

- Issue 1 focused on extraction only; full unified `QuantumScenePrimitives.tsx` file was deemed too risky (god file) and not implemented
- Algorithm-specific scene rendering (BlochSphereNode, SignalBar, Graph layout) remains per-algorithm
- `HybridFlowPanel` is currently QAOA-specific but could be made generic for VQE when VQE animation is built
- All changes follow existing code style and patterns